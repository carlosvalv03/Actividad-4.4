public class Prestamo {
    private int id;

    public Prestamo(int id) {
        this.id = id;
    }

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }
}

/**
 * Cambios realizados en el código
 * Agrega un campo id que almacena el identificador del préstamo.
 * Agrega un constructor que acepta un parámetro id y lo usa para inicializar el campo id.
 * Agrega los métodos getId() y setId() para obtener y establecer el valor del campo id.
 */